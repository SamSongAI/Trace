# Trace 产品愿景：私有上下文层

> Trace 不是笔记应用。Trace 是一个私有的 X（Twitter）——
> 每条 trace 就是一条"帖子"，持续积累，供 AI 和 IT 系统消费。

---

## 核心类比

| X / Twitter | Trace |
|---|---|
| 发帖（Tweet） | 写一条 trace |
| 公开时间线 | 本地 Markdown 文件 |
| 关注 / 列表 | 分类（Section）/ 线程（Thread）|
| 转发 / 引用 | 追加到上一条（⌘⇧Enter）|
| 媒体附件（图片/GIF） | 粘贴/拖拽图片 → Markdown 引用 |
| 公开广播 | 私有积累，写入本地文件 |
| 人消费 | AI / IT 系统消费 |
| 算法分发 | 路径分发（选择写入哪个文件）|

**一句话定位：** Trace 是给 AI 消费的私有 Twitter。每条 trace 是一个上下文片段，按路径分发到不同的 Markdown 文件中，持续积累形成你的私有上下文库。

---

## 产品理念

### 1. 捕获体验 = 发帖体验

X 的发帖体验是互联网产品中做得最好的之一：
- 打开即写，无需思考"写到哪"
- 输入即反馈，图片直接内联预览
- 一键发送，Enter 即可
- 发送后有明确的视觉反馈（动画 + toast）
- 草稿持久化，关闭不丢失

**Trace 必须对齐这个体验。** 用户唤起面板的那一刻，就应该像打开 X 的发帖框一样——不需要思考，只需要输入。

### 2. 路径分发 = 选择受众

在 X 上，你发帖时 implicitly 选择了"所有人"这个受众。
在 Trace 上，你发帖时 explicit 选择一个路径——日记的某个分类、某个线程、某个独立文档。

**路径选择不应该增加摩擦。** 当前的 section/thread 按钮已经是很好的设计，但可以更接近 X 的"选择受众"下拉菜单的体验——默认记住上次选择，一键切换。

### 3. 持续积累 = 时间线

X 的核心价值是时间线——你的帖子持续积累，形成你的数字身份。
Trace 的核心价值同样是积累——你的 trace 持续写入文件，形成你的私有上下文库。

**用户应该能"看到"积累。** 当前 Trace 写完即走，没有可视化的"已发送"反馈。下一版应该让用户感受到积累——比如发送后短暂显示最近几条 trace，或者面板展开时显示今天的时间线。

### 4. AI 消费 = 上下文工程

X 的帖子被人消费，Trace 的帖子被 AI 消费。
这意味着 trace 的格式需要是 AI 友好的：
- Markdown 原生（已实现）
- 图片引用是相对路径（已实现）
- 时间戳自动添加（已实现）
- 结构化分类（已实现）

未来可以考虑：
- 支持 frontmatter / tags，让 AI 更容易索引
- 支持 `[[wikilink]]` 语法，让 trace 之间产生关联
- 支持代码块语法高亮预览

---

## UI/UX 改进方向

### 当前结构

```
┌─────────────────────────────────┐
│ Header: Logo | Pin | Settings   │  36px
├─────────────────────────────────┤
│                                 │
│ Editor (文本输入区)              │  自适应
│                                 │
├─────────────────────────────────┤
│ [缩略图条] (有图片时显示)         │  60px
├─────────────────────────────────┤
│ Footer: [分类/线程/文档标题]      │  ~50px
└─────────────────────────────────┘
```

### 下一版目标结构

```
┌─────────────────────────────────┐
│ Header: Logo | 路径选择 | Pin ⚙  │  36px
│         (路径选择替代底部按钮)     │
├─────────────────────────────────┤
│                                 │
│ Editor (文本输入区)              │
│ ┌─────────────────────────────┐ │
│ │ [图片缩略图内联预览]          │ │
│ └─────────────────────────────┘ │
│                                 │
├─────────────────────────────────┤
│ 发送栏: [⌘Enter 发送] [计数]    │  32px
├─────────────────────────────────┤
│ 最近 traces (可选展开)           │  折叠
│ • 14:32  想法  "关于X的思考..."  │
│ • 14:28  灵感  "如果Trace能..."  │
│ • 14:15  待办  "明天需要..."     │
└─────────────────────────────────┘
```

### 关键改进点

#### P0 — 发帖体验对齐

1. **发送反馈动画** — 保存成功后，编辑器内容以淡出动画消失（而非瞬间清空），配合 toast "已保存到 [分类]"
2. **草稿持久化** — 面板关闭时未保存的文本保留，下次打开恢复
3. **图片内联预览** — 粘贴的图片在编辑器内显示为缩略图行（当前已实现缩略图条，可进一步内联到光标位置）
4. **字数/行数提示** — 编辑器右下角显示轻量字数，不强制限制

#### P1 — 路径选择优化

5. **路径选择器上移** — 将分类/线程选择从底部按钮移到 header 区域，类似 X 的"选择受众"下拉菜单
6. **记住上次选择** — 默认使用上次的选择，无需每次手动切换
7. **快捷切换** — ⌘1-9 快速切换路径（已实现）

#### P2 — 积累可视化

8. **最近 traces 预览** — 面板底部可折叠区域，显示今天最近 3-5 条 trace 的摘要（时间 + 分类 + 首行）
9. **发送后短暂展示** — 保存成功后，新 trace 在底部预览区高亮闪现，然后自动折叠
10. **每日统计** — header 或设置中显示"今天已记录 N 条"

#### P3 — 高级特性

11. **线程视图** — Thread 模式下展开可看到该线程的最近条目
12. **Markdown 实时预览** — 输入 `#` `**` `[]` 等语法时轻量预览
13. **全局快捷输入** — 类似 Raycast/Alfred 的全屏快速输入，不限于浮动面板

---

## 设计原则（更新）

在原有设计原则基础上：

- **捕获优先于整理** — 先留住想法，分类是后面的事
- **零摩擦** — 全局快捷键唤起，没有加载时间，没有多余的步骤
- **本地优先** — 没有账号，没有云端，没有遥测
- **轻量聚焦** — 不做编辑器，不做知识库。只把"记下来"这件事做到极致
- **发帖体验** — 每次捕获都应该像发一条推文一样轻松、即时、有反馈
- **积累可见** — 用户应该能感受到自己的 trace 在持续积累，形成上下文资产
- **AI 友好** — 输出格式为 AI 消费优化，Markdown 原生，结构化，可索引
