//! Executable entry point for the Trace Windows client.
//!
//! Phase 0 placeholder: prints the crate version and exits. Real wiring of
//! the iced UI, tray icon and global hotkeys happens in later phases.

fn main() {
    println!("trace-app {} — scaffold only", trace_core::VERSION);
}
